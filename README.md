# 무신사페이먼츠 포인트 시스템

## 목차

1. [프로젝트 개요](#프로젝트-개요)
2. [기술 스택](#기술-스택)
3. [프로젝트 구조](#프로젝트-구조)
4. [빌드 방법](#빌드-방법)
5. [실행 방법](#실행-방법)
6. [API 문서](#api-문서)
7. [과제 요구사항 충족 사항](#과제-요구사항-충족-사항)
8. [ERD 및 아키텍처](#erd-및-아키텍처)
9. [개발 가이드](#개발-가이드)

---

## 프로젝트 개요

무신사페이먼츠 무료 포인트 시스템은 사용자에게 포인트를 적립하고, 사용하며, 취소할 수 있는 기능을 제공하는 백엔드 API 시스템입니다.

### 주요 기능

- **포인트 적립**: 사용자에게 포인트를 적립하고, 1원 단위로 사용 추적
- **포인트 적립 취소**: 사용되지 않은 적립 건 취소
- **포인트 사용**: 주문 시 포인트 사용 (수기 지급 우선, 만료일 짧은 순)
- **포인트 사용 취소**: 사용한 포인트 전체/부분 취소 (만료 포인트는 신규 적립 처리)
- **포인트 잔액 조회**: 사용자의 포인트 잔액 및 적립 내역 조회
- **포인트 사용 내역 조회**: 주문별 포인트 사용 내역 조회

### 핵심 특징

- **1원 단위 추적**: 어떤 적립 건에서 얼마를 사용했는지 1원 단위로 추적 가능
- **동적 설정 관리**: 하드코딩 없이 설정 값 변경 가능 (1회 최대 적립 금액, 개인별 최대 보유 금액 등)
- **만료일 관리**: 모든 포인트는 만료일을 가지며, 만료일 범위 검증 (1일 이상, 5년 미만)
- **수기 지급 구분**: 관리자가 수기로 지급한 포인트와 일반 적립 구분
- **사용 우선순위**: 수기 지급 포인트 우선, 그 다음 만료일 짧은 순으로 사용

---

## 기술 스택

### 개발 환경

- **Java**: 21
- **Spring Boot**: 3.5.7 (Stable)
- **Spring Data JPA**: 데이터 영속성
- **H2 Database**: 개발 환경 데이터베이스

### 빌드 도구

- **Gradle**: 8.x 이상

### 테스트

- **Kotest**: Kotlin 전용 테스트 프레임워크
  - 다양한 테스트 스타일 지원 (String Spec, Behavior Spec, Fun Spec 등)
  - 내장 어설션 라이브러리
  - MockK 통합 지원

### 문서화

- **SpringDoc OpenAPI 3**: API 문서 자동 생성 (Swagger)

### 아키텍처

- **헥사고날 아키텍처 (Hexagonal Architecture)**: 도메인 중심 설계

---

## 프로젝트 구조

본 프로젝트는 헥사고날 아키텍처(Hexagonal Architecture)를 적용하여 설계되었습니다.

```
com.musinsa.payments.point
├── domain              # 도메인 레이어 (핵심 비즈니스 로직)
│   ├── entity          # 도메인 엔티티
│   ├── valueobject     # 값 객체 (Money, OrderNumber, PointKey)
│   ├── repository      # 리포지토리 인터페이스 (포트)
│   ├── service         # 도메인 서비스
│   └── exception       # 도메인 예외
├── port                # 포트 레이어
│   ├── in              # 인바운드 포트 (Use Case 인터페이스)
│   └── out             # 아웃바운드 포트 (영속성, 설정 등)
├── adapter             # 어댑터 레이어
│   ├── in              # 인바운드 어댑터
│   │   └── web         # REST API 컨트롤러
│   └── out             # 아웃바운드 어댑터
│       └── persistence # JPA 구현
└── service             # 애플리케이션 서비스
```

자세한 설계 문서는 `docs/` 디렉토리를 참고하세요.

---

## 빌드 방법

### 사전 요구사항

- **JDK**: 21 이상
- **Gradle**: 8.x 이상 (또는 Gradle Wrapper 사용)

### 빌드 명령어

```bash
# 프로젝트 클론
git clone <repository-url>
cd musinsa-payments-2025

# Gradle Wrapper를 사용한 빌드
./gradlew build

# 테스트 제외하고 빌드
./gradlew build -x test

# JAR 파일 생성
./gradlew bootJar
```

### 빌드 결과물

- `build/libs/musinsa-payments-point-1.0.0.jar`: 실행 가능한 JAR 파일

---

## 실행 방법

### 로컬 실행

```bash
# JAR 파일 실행
java -jar build/libs/musinsa-payments-point-1.0.0.jar

# 또는 Gradle로 직접 실행
./gradlew bootRun
```

### 애플리케이션 설정

애플리케이션은 기본적으로 다음 포트에서 실행됩니다:

- **포트**: 8080
- **H2 콘솔**: http://localhost:8080/h2-console
- **Swagger UI**: http://localhost:8080/swagger-ui.html
- **API 문서 (JSON)**: http://localhost:8080/v3/api-docs

### 데이터베이스 접속 정보 (H2)

- **JDBC URL**: `jdbc:h2:mem:testdb`
- **Username**: `sa`
- **Password**: (비밀번호 없음)

### 초기 데이터

애플리케이션 시작 시 다음 설정이 자동으로 초기화됩니다:

- `MAX_ACCUMULATION_AMOUNT_PER_TIME`: 100000 (1회 최대 적립 금액)
- `MAX_BALANCE_PER_MEMBER`: 10000000 (개인별 최대 보유 금액)
- `DEFAULT_EXPIRATION_DAYS`: 365 (기본 만료일)
- `MIN_EXPIRATION_DAYS`: 1 (최소 만료일)
- `MAX_EXPIRATION_DAYS`: 1824 (최대 만료일, 약 5년)

---

## API 문서

### Swagger UI

애플리케이션 실행 후 다음 URL에서 API 문서를 확인할 수 있습니다:

- **Swagger UI**: http://localhost:8080/swagger-ui.html

### 주요 API 엔드포인트

#### 1. 포인트 적립

- **POST** `/api/points/accumulate`
- **Request Body**:
```json
{
  "memberId": 1,
  "amount": 1000,
  "expirationDays": 365,
  "isManualGrant": false
}
```

#### 2. 포인트 적립 취소

- **POST** `/api/points/accumulate/{pointKey}/cancel`
- **Path Parameter**: `pointKey` - 취소할 적립 건의 포인트 키
- **Request Body**:
```json
{
  "reason": "적립 오류"
}
```

#### 3. 포인트 사용

- **POST** `/api/points/use`
- **Request Body**:
```json
{
  "memberId": 1,
  "orderNumber": "A1234",
  "amount": 1200
}
```

#### 4. 포인트 사용 취소

- **POST** `/api/points/use/{pointKey}/cancel`
- **Path Parameter**: `pointKey` - 취소할 사용 건의 포인트 키
- **Request Body**:
```json
{
  "amount": 1100,
  "reason": "주문 취소"
}
```

#### 5. 포인트 잔액 조회

- **GET** `/api/points/balance/{memberId}`
- **Path Parameter**: `memberId` - 사용자 ID

#### 6. 포인트 사용 내역 조회

- **GET** `/api/points/history/{memberId}?page=0&size=20&orderNumber=A1234`
- **Path Parameter**: `memberId` - 사용자 ID
- **Query Parameters**:
  - `page`: 페이지 번호 (기본값: 0)
  - `size`: 페이지 크기 (기본값: 20)
  - `orderNumber`: 주문번호로 필터링 (옵션)

### API 사용 예시

#### cURL 예시

```bash
# 포인트 적립
curl -X POST http://localhost:8080/api/points/accumulate \
  -H "Content-Type: application/json" \
  -d '{
    "memberId": 1,
    "amount": 1000,
    "expirationDays": 365,
    "isManualGrant": false
  }'

# 포인트 사용
curl -X POST http://localhost:8080/api/points/use \
  -H "Content-Type: application/json" \
  -d '{
    "memberId": 1,
    "orderNumber": "A1234",
    "amount": 1200
  }'

# 포인트 잔액 조회
curl -X GET http://localhost:8080/api/points/balance/1

# 포인트 사용 내역 조회
curl -X GET "http://localhost:8080/api/points/history/1?page=0&size=20"
```

---

## 과제 요구사항 충족 사항

### ✅ 필수 요구사항

#### 1. 기능 구현

##### 적립 기능
- [x] 1회 적립 가능 범위: 1원 이상, 10만원 이하 (동적 설정)
- [x] 개인별 최대 보유 금액 제한 (동적 설정)
- [x] 1원 단위 포인트 사용 추적 (`PointUsageDetail` 엔티티)
- [x] 수기 지급 포인트 구분 (`isManualGrant` 필드)
- [x] 만료일 관리 (1일 이상, 5년 미만, 기본 365일)

##### 적립 취소 기능
- [x] 전체 금액 취소 (사용된 금액이 있으면 취소 불가)

##### 사용 기능
- [x] 주문번호와 함께 사용 내역 기록
- [x] 사용 우선순위: 수기 지급 > 만료일 짧은 순

##### 사용 취소 기능
- [x] 전체/부분 취소 지원
- [x] 만료된 포인트 사용 취소 시 신규 적립 처리

#### 2. 기술 스택
- [x] Java 21
- [x] Spring Boot 3.5.7 (Stable)
- [x] H2 Database

#### 3. 문서화
- [x] ERD 다이어그램 (resource 폴더)
- [x] AWS 아키텍처 다이어그램 (resource 폴더, 옵션)
- [x] README.md에 빌드 방법 및 과제 설명

### 📋 구현 상세

#### 동적 설정 관리

`PointConfig` 엔티티를 통해 다음 설정을 동적으로 관리합니다:

- `MAX_ACCUMULATION_AMOUNT_PER_TIME`: 1회 최대 적립 금액 (기본: 100000원)
- `MAX_BALANCE_PER_MEMBER`: 개인별 최대 보유 금액 (기본: 10000000원)
- `DEFAULT_EXPIRATION_DAYS`: 기본 만료일 (기본: 365일)
- `MIN_EXPIRATION_DAYS`: 최소 만료일 (기본: 1일)
- `MAX_EXPIRATION_DAYS`: 최대 만료일 (기본: 1824일, 약 5년)

설정은 데이터베이스에 저장되며, 런타임에 변경 가능합니다.

#### 포인트 추적

`PointUsageDetail` 엔티티를 통해 1원 단위로 포인트 사용을 추적합니다:

- 어떤 적립 건(`PointAccumulation`)에서 얼마를 사용했는지 상세히 기록
- 주문번호와 함께 사용 내역 관리
- 사용 취소 시에도 상세 내역 단위로 취소 처리

#### 수기 지급 포인트

- `PointAccumulation.isManualGrant` 필드로 구분
- 포인트 사용 시 수기 지급 포인트가 우선 사용됨
- 적립 시 `isManualGrant: true`로 설정

#### 만료일 관리

- 모든 포인트는 만료일(`expirationDate`)을 가짐
- 만료일 범위: 1일 이상, 5년(1824일) 미만
- 기본 만료일: 365일
- 만료일 검증은 적립 시점에 수행

#### 사용 우선순위

포인트 사용 시 다음 우선순위로 사용됩니다:

1. **수기 지급 포인트** (`isManualGrant = true`)
2. **만료일이 짧은 순** (FIFO - First In First Out)

#### 만료 포인트 처리

사용 취소 시점에 이미 만료된 포인트는 신규 적립으로 처리됩니다:

- 만료된 포인트는 사용 가능 잔액을 복원할 수 없으므로 신규 적립 처리
- 새로운 `pointKey`로 적립 처리
- 기본 만료일로 새로운 만료일 설정

### 📝 예시 시나리오

과제 요구사항의 예시 시나리오를 구현했습니다:

1. **1000원 적립** (pointKey: A) → 총 잔액: 0 → 1000원
2. **500원 적립** (pointKey: B) → 총 잔액: 1000 → 1500원
3. **주문번호 A1234에서 1200원 사용** (pointKey: C)
   - A 적립에서 1000원 사용 → A의 사용 가능 잔액: 1000 → 0원
   - B 적립에서 200원 사용 → B의 사용 가능 잔액: 500 → 300원
   - 총 잔액: 1500 → 300원
4. **A 적립 만료**
5. **C의 사용금액 1200원 중 1100원 부분 사용 취소** (pointKey: D)
   - A는 만료되었으므로 1000원을 신규 적립 처리 (pointKey: E)
   - B는 만료되지 않았으므로 사용 가능 잔액 복원: 300 → 400원
   - 총 잔액: 300 → 1400원
   - C는 이제 1200원 사용금액 중 100원을 추가 취소 가능

---

## ERD 및 아키텍처

### ERD 다이어그램

ERD 다이어그램은 `resource/` 디렉토리에 저장되어 있습니다:

- **PNG 이미지**: `resource/erd.png`
- **SVG 이미지**: `resource/erd.svg` (벡터 이미지, 권장)
- **PDF 문서**: `resource/erd.pdf`
- **Mermaid 소스**: `resource/erd.mmd`

ERD 설계 문서: [`docs/02-ERD-설계.md`](docs/02-ERD-설계.md)

### AWS 아키텍처 다이어그램

AWS 기반 아키텍처 설계 문서: [`docs/08-AWS-아키텍처-설계.md`](docs/08-AWS-아키텍처-설계.md)

### 설계 문서

모든 설계 문서는 `docs/` 디렉토리에 있습니다:

1. [`01-도메인-분석.md`](docs/01-도메인-분석.md): 도메인 분석 및 요구사항 정리
2. [`02-ERD-설계.md`](docs/02-ERD-설계.md): 데이터베이스 설계
3. [`03-도메인-모델-설계.md`](docs/03-도메인-모델-설계.md): 도메인 모델 설계
4. [`04-API-설계.md`](docs/04-API-설계.md): REST API 설계
5. [`05-비즈니스-로직-설계.md`](docs/05-비즈니스-로직-설계.md): 비즈니스 로직 설계
6. [`06-설정-관리-설계.md`](docs/06-설정-관리-설계.md): 설정 관리 설계
7. [`07-프로젝트-구조-설계.md`](docs/07-프로젝트-구조-설계.md): 헥사고날 아키텍처 기반 프로젝트 구조
8. [`08-AWS-아키텍처-설계.md`](docs/08-AWS-아키텍처-설계.md): AWS 아키텍처 설계
9. [`09-테스트-전략.md`](docs/09-테스트-전략.md): 테스트 전략
10. [`10-문서화-계획.md`](docs/10-문서화-계획.md): 문서화 계획

---

## 개발 가이드

### 코드 스타일

- Google Java Style Guide 준수
- Kotlin 코딩 컨벤션 준수

### 테스트 실행

본 프로젝트는 **Kotest**를 사용하여 테스트 코드를 작성합니다.

```bash
# 모든 테스트 실행
./gradlew test

# 특정 테스트 클래스 실행
./gradlew test --tests "PointAccumulationTest"

# 커버리지 리포트 생성
./gradlew test jacocoTestReport
```

**Kotest 특징**:
- Kotlin DSL 기반의 직관적인 테스트 작성
- 다양한 테스트 스타일 지원 (String Spec, Behavior Spec, Fun Spec 등)
- 내장 어설션 라이브러리로 간결한 검증 코드 작성
- MockK와의 통합으로 모킹 지원

### 코드 커버리지

- **목표**: 라인 커버리지 80% 이상
- **리포트 위치**: `build/reports/jacoco/test/html/index.html`

### 프로젝트 구조 설명

본 프로젝트는 **헥사고날 아키텍처(Hexagonal Architecture)**를 적용하여 설계되었습니다:

- **도메인 레이어**: 핵심 비즈니스 로직 (엔티티, 값 객체, 도메인 서비스)
- **포트 레이어**: 인터페이스 정의 (인바운드/아웃바운드)
- **어댑터 레이어**: 외부 시스템과의 연결 (웹, 영속성 등)
- **서비스 레이어**: 애플리케이션 서비스 (Use Case 구현)

자세한 내용은 [`docs/07-프로젝트-구조-설계.md`](docs/07-프로젝트-구조-설계.md)를 참고하세요.

---

## 라이선스

이 프로젝트는 무신사페이먼츠 Backend Engineer 과제 전형을 위한 프로젝트입니다.

---

## 문의

프로젝트에 대한 문의사항이 있으시면 이슈를 등록해 주세요.
