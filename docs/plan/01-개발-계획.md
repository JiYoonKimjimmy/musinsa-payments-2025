# 무신사페이먼츠 포인트 시스템 개발 계획

## 목차

1. [개요](#개요)
2. [개발 단계](#개발-단계)
   - [1단계: 프로젝트 초기 설정 및 인프라 구축](#1단계-프로젝트-초기-설정-및-인프라-구축)
   - [2단계: 도메인 모델 구현](#2단계-도메인-모델-구현)
   - [3단계: 인프라스트럭처 레이어 구현](#3단계-인프라스트럭처-레이어-구현)
   - [4단계: 포트 레이어 구현](#4단계-포트-레이어-구현)
   - [5단계: 애플리케이션 서비스 구현](#5단계-애플리케이션-서비스-구현)
   - [6단계: 웹 어댑터 구현](#6단계-웹-어댑터-구현)
   - [7단계: 설정 관리 구현](#7단계-설정-관리-구현)
   - [8단계: 테스트 구현](#8단계-테스트-구현)
   - [9단계: 문서화 및 배포 준비](#9단계-문서화-및-배포-준비)
3. [단계별 검토 체크리스트](#단계별-검토-체크리스트)

---

## 개요

본 문서는 무신사페이먼츠 포인트 시스템의 단계별 개발 계획을 정의합니다. 각 단계는 명확한 개발 항목과 체크리스트를 포함하며, 단계별 완료 시 검토 후 다음 단계로 진행합니다.

### 개발 원칙

- **헥사고날 아키텍처** 적용: 도메인 중심 설계
- **점진적 개발**: 단계별 검증 후 다음 단계 진행
- **테스트 주도**: 각 단계별 테스트 코드 작성
- **문서화**: 각 단계별 설계 문서 참고

---

## 개발 단계

### 1단계: 프로젝트 초기 설정 및 인프라 구축

**목표**: 프로젝트 기본 구조 설정 및 개발 환경 구축

#### 개발 항목

- [x] Gradle 프로젝트 설정
  - [x] Java 21 설정
  - [x] Spring Boot 3.5.7 의존성 추가
  - [x] Spring Data JPA 의존성 추가
  - [x] H2 Database 의존성 추가
  - [x] Kotest 의존성 추가 (테스트 프레임워크)
  - [x] SpringDoc OpenAPI 3 의존성 추가 (Swagger)
  - [x] JaCoCo 플러그인 설정 (코드 커버리지)
- [x] 프로젝트 디렉토리 구조 생성
  - [x] 헥사고날 아키텍처 기반 패키지 구조 생성
    - `domain/entity` - 도메인 엔티티
    - `domain/valueobject` - 값 객체
    - `domain/service` - 도메인 서비스
    - `domain/exception` - 도메인 예외
    - `application/port/in` - 인바운드 포트 (Use Case 인터페이스)
    - `application/port/out` - 아웃바운드 포트 (영속성, 설정 등)
    - `application/service` - 애플리케이션 서비스 (Use Case 구현)
    - `infrastructure/persistence` - 영속성 구현 (JPA, 어댑터, 매퍼)
    - `infrastructure/config` - 설정 어댑터
    - `presentation/web` - 웹 어댑터 (컨트롤러, DTO, 매퍼)
- [x] 애플리케이션 설정 파일 작성
  - [x] `application.yml` 작성
  - [x] H2 Database 설정
  - [x] JPA 설정
  - [x] Swagger 설정
- [x] 기본 애플리케이션 클래스 작성
  - [x] `@SpringBootApplication` 메인 클래스
  - [x] 패키지 스캔 설정

#### 체크리스트

- [x] 프로젝트가 정상적으로 빌드되는가?
- [x] 애플리케이션이 정상적으로 실행되는가?
- [x] H2 콘솔에 접속할 수 있는가? (http://localhost:8080/h2-console)
- [x] Swagger UI에 접속할 수 있는가? (http://localhost:8080/swagger-ui.html)

#### 참고 문서

- [`docs/07-프로젝트-구조-설계.md`](../docs/07-프로젝트-구조-설계.md)

---

### 2단계: 도메인 모델 구현

**목표**: 핵심 도메인 모델 및 비즈니스 로직 구현

#### 개발 항목

##### 2.1 값 객체 (Value Object) 구현

- [x] `Money` 값 객체 구현
  - [x] 금액 생성 및 검증 로직
  - [x] 금액 덧셈/뺄셈 메서드
  - [x] 불변성 보장
- [x] `PointKey` 값 객체 구현
  - [x] 포인트 키 생성 로직
  - [x] 고유성 보장
- [x] `OrderNumber` 값 객체 구현
  - [x] 주문번호 검증 로직

##### 2.2 도메인 엔티티 구현

- [x] `PointAccumulation` 엔티티 구현
  - [x] 기본 필드 (pointKey, memberId, amount, availableAmount 등)
  - [x] 만료일 관리 (expirationDate)
  - [x] 수기 지급 구분 (isManualGrant)
  - [x] 상태 관리 (status)
  - [x] 포인트 사용 메서드 (`use()`)
  - [x] 취소 가능 여부 확인 메서드 (`canBeCancelled()`)
  - [x] 만료 여부 확인 메서드 (`isExpired()`, `isExpiredAt()`)
  - [x] 포인트 복원 메서드 (`restore()`)
  - [x] 만료 처리 메서드 (`markAsExpired()`)
- [x] `PointUsage` 엔티티 구현
  - [x] 기본 필드 (pointKey, memberId, orderNumber, totalAmount, cancelledAmount 등)
  - [x] 상태 관리 (status)
  - [x] 취소 가능 여부 확인 메서드 (`canCancel()`)
  - [x] 사용 취소 메서드 (`cancel()`)
  - [x] 남은 사용 금액 계산 메서드 (`getRemainingAmount()`)
- [x] `PointUsageDetail` 엔티티 구현
  - [x] 1원 단위 추적을 위한 상세 내역
  - [x] 적립 건과의 연관 관계 (pointAccumulationId)
  - [x] 사용 건과의 연관 관계 (pointUsageId)
  - [x] 상세 내역 취소 메서드 (`cancel()`)
  - [x] 전체 취소 여부 확인 메서드 (`isFullyCancelled()`)
- [x] `PointConfig` 엔티티 구현
  - [x] 설정 키 (configKey)
  - [x] 설정 값 (configValue)
  - [x] 동적 설정 관리 로직 (getLongValue, getIntValue, getBooleanValue, updateConfigValue)

##### 2.3 도메인 예외 구현

- [x] `PointDomainException` 추상 클래스
- [x] `InsufficientPointException` (잔액 부족)
- [x] `InvalidAmountException` (잘못된 금액)
- [x] `PointExpiredException` (만료된 포인트)
- [x] `InvalidExpirationDateException` (잘못된 만료일)
- [x] `MaxBalanceExceededException` (최대 보유 금액 초과)
- [x] `MaxAccumulationExceededException` (최대 적립 금액 초과)
- [x] `CannotCancelAccumulationException` (적립 취소 불가)
- [x] `CannotCancelUsageException` (사용 취소 불가)
- [x] `CannotCancelDetailException` (상세 내역 취소 불가)

##### 2.4 도메인 서비스 구현

- [x] `PointUsagePriorityService` 구현
  - [x] 포인트 사용 우선순위 로직
    - [x] 수기 지급 포인트 우선
    - [x] 만료일 짧은 순서 정렬
  - [x] 포인트 선택 알고리즘 (`selectAccumulationsForUsage()`)
  - [x] 사용 가능한 적립 건 필터링 (사용 가능 잔액 있음 && 만료되지 않음)

#### 체크리스트

- [x] 모든 값 객체가 불변성을 보장하는가? (Money는 private constructor, PointKey/OrderNumber는 data class)
- [x] 도메인 엔티티의 비즈니스 로직이 올바르게 구현되었는가? (모든 비즈니스 메서드 구현 완료)
- [x] 도메인 예외가 적절히 정의되었는가? (10개 도메인 예외 구현 완료)
- [x] 포인트 사용 우선순위 로직이 정확한가? (수기 지급 우선, 만료일 짧은 순 정렬)

#### 테스트 항목

- [x] 값 객체 단위 테스트 작성 (MoneyTest, PointKeyTest, OrderNumberTest - 총 27개 테스트 케이스)
- [x] 도메인 엔티티 단위 테스트 작성 (PointAccumulationTest, PointUsageTest, PointUsageDetailTest, PointConfigTest - 총 51개 테스트 케이스)
- [x] 도메인 서비스 단위 테스트 작성 (PointUsagePriorityServiceTest - 총 7개 테스트 케이스)

#### 참고 문서

- [`docs/01-도메인-분석.md`](../docs/01-도메인-분석.md)
- [`docs/03-도메인-모델-설계.md`](../docs/03-도메인-모델-설계.md)
- [`docs/05-비즈니스-로직-설계.md`](../docs/05-비즈니스-로직-설계.md)

---

### 3단계: 인프라스트럭처 레이어 구현

**목표**: 데이터 영속성을 위한 JPA 어댑터 구현

#### 개발 항목

##### 3.1 JPA 엔티티 구현

- [x] `PointAccumulationEntity` 구현
  - [x] JPA 엔티티 매핑
  - [x] 데이터베이스 테이블 매핑
  - [x] 연관 관계 설정
- [x] `PointUsageEntity` 구현
  - [x] JPA 엔티티 매핑
  - [x] 데이터베이스 테이블 매핑
  - [x] 연관 관계 설정
- [x] `PointUsageDetailEntity` 구현
  - [x] JPA 엔티티 매핑
  - [x] 데이터베이스 테이블 매핑
  - [x] 연관 관계 설정
- [x] `PointConfigEntity` 구현
  - [x] JPA 엔티티 매핑
  - [x] 데이터베이스 테이블 매핑

##### 3.2 JPA 리포지토리 구현

- [x] `PointAccumulationJpaRepository` 인터페이스 (이름 변경: Jpa 접두사 → JpaRepository 접미사)
  - [x] `findByPointKey()` 메서드
  - [x] `findByMemberIdAndStatus()` 메서드
  - [x] `findAvailableAccumulationsByMemberId()` 메서드 (수기 지급 우선, 만료일 짧은 순 정렬)
  - [x] `sumAvailableAmountByMemberId()` 메서드
- [x] `PointUsageJpaRepository` 인터페이스
  - [x] `findByPointKey()` 메서드
  - [x] `findByMemberIdAndOrderNumber()` 메서드
  - [x] `findByMemberId()` 메서드 (페이징)
- [x] `PointUsageDetailJpaRepository` 인터페이스
  - [x] `findByPointUsageId()` 메서드
  - [x] `findByPointAccumulationId()` 메서드
  - [x] `findByUsagePointKey()` 메서드 (JPQL join)
  - [x] `findByAccumulationPointKey()` 메서드 (JPQL join)
- [x] `PointConfigJpaRepository` 인터페이스
  - [x] `findByConfigKey()` 메서드
  - [x] `findAll()` 메서드 (JpaRepository 상속)

##### 3.3 Entity Mapper 구현

- [x] `PointEntityMapper` 구현
  - [x] 도메인 엔티티 ↔ JPA 엔티티 변환
  - [x] `PointAccumulation` ↔ `PointAccumulationEntity`
  - [x] `PointUsage` ↔ `PointUsageEntity`
  - [x] `PointUsageDetail` ↔ `PointUsageDetailEntity`
  - [x] `PointConfig` ↔ `PointConfigEntity`

##### 3.4 Persistence Adapter 구현

- [x] `PointAccumulationPersistenceAdapter` 구현
  - [x] Infrastructure 레이어의 `infrastructure.persistence.adapter` 패키지에 위치
  - [x] `PointAccumulationPersistencePort` 인터페이스 구현
  - [x] `save()` 메서드
  - [x] `findByPointKey()` 메서드
  - [x] `findByMemberIdAndStatus()` 메서드
  - [x] `findAvailableAccumulationsByMemberId()` 메서드
  - [x] `sumAvailableAmountByMemberId()` 메서드
- [x] `PointUsagePersistenceAdapter` 구현
  - [x] Infrastructure 레이어의 `infrastructure.persistence.adapter` 패키지에 위치
  - [x] `PointUsagePersistencePort` 인터페이스 구현
  - [x] `save()` 메서드
  - [x] `findByPointKey()` 메서드
  - [x] `findByMemberIdAndOrderNumber()` 메서드
  - [x] `findUsageHistoryByMemberId()` 메서드 (페이징)
- [x] `PointUsageDetailPersistenceAdapter` 구현
  - [x] Infrastructure 레이어의 `infrastructure.persistence.adapter` 패키지에 위치
  - [x] `PointUsageDetailPersistencePort` 인터페이스 구현
  - [x] `saveAll()` 메서드
  - [x] `findByUsagePointKey()` 메서드
  - [x] `findByAccumulationPointKey()` 메서드
- [x] `PointConfigAdapter` 구현
  - [x] Infrastructure 레이어의 `infrastructure.config` 패키지에 위치
  - [x] `PointConfigPort` 인터페이스 구현
  - [x] `findByConfigKey()` 메서드
  - [x] `findAll()` 메서드
  - [x] `save()` 메서드

##### 3.5 데이터베이스 초기화

- [x] `DataInitializer` 구현
  - [x] 애플리케이션 시작 시 기본 설정 데이터 초기화
  - [x] `MAX_ACCUMULATION_AMOUNT_PER_TIME`: 100000
  - [x] `MAX_BALANCE_PER_MEMBER`: 10000000
  - [x] `DEFAULT_EXPIRATION_DAYS`: 365
  - [x] `MIN_EXPIRATION_DAYS`: 1
  - [x] `MAX_EXPIRATION_DAYS`: 1824

#### 체크리스트

- [x] 모든 JPA 엔티티가 올바르게 매핑되었는가?
- [x] 리포지토리 메서드가 정상적으로 동작하는가?
- [x] 도메인 엔티티와 JPA 엔티티 간 변환이 정확한가?
- [x] 초기 데이터가 정상적으로 생성되는가?

#### 테스트 항목

- [x] JPA 리포지토리 통합 테스트 작성
  - [x] PointAccumulationJpaRepositoryTest (8개 테스트 케이스)
  - [x] PointUsageJpaRepositoryTest (5개 테스트 케이스)
  - [x] PointUsageDetailJpaRepositoryTest (5개 테스트 케이스)
  - [x] PointConfigJpaRepositoryTest (5개 테스트 케이스)
- [x] Persistence Adapter 통합 테스트 작성
  - [x] PointAccumulationPersistenceAdapterTest (9개 테스트 케이스)
  - [x] PointUsagePersistenceAdapterTest (8개 테스트 케이스)
  - [x] PointUsageDetailPersistenceAdapterTest (6개 테스트 케이스)
  - [x] PointConfigAdapterTest (7개 테스트 케이스)
- [x] 데이터 초기화 테스트 작성 (DataInitializerTest - 9개 테스트 케이스, @DataJpaTest 사용)

#### 참고 문서

- [`docs/02-ERD-설계.md`](../docs/02-ERD-설계.md)

---

### 4단계: 포트 레이어 구현

**목표**: 인바운드/아웃바운드 포트 인터페이스 정의

#### 개발 항목

##### 4.1 인바운드 포트 (Use Case 인터페이스) 구현

- [x] `PointAccumulationUseCase` 인터페이스
  - [x] `accumulate()` 메서드 정의
  - [x] `cancelAccumulation()` 메서드 정의
- [x] `PointUsageUseCase` 인터페이스
  - [x] `use()` 메서드 정의
- [x] `PointCancellationUseCase` 인터페이스
  - [x] `cancelUsage()` 메서드 정의
- [x] `PointQueryUseCase` 인터페이스
  - [x] `getBalance()` 메서드 정의
  - [x] `getUsageHistory()` 메서드 정의 (페이징)

##### 4.2 아웃바운드 포트 (영속성, 설정 등 인터페이스) 구현

- [x] `PointAccumulationPersistencePort` 인터페이스
  - [x] Application 레이어의 `application.port.output.persistence` 패키지에 위치
  - [x] 저장, 조회 메서드 정의
  - [x] 도메인 엔티티를 사용하는 인터페이스
- [x] `PointUsagePersistencePort` 인터페이스
  - [x] Application 레이어의 `application.port.output.persistence` 패키지에 위치
  - [x] 저장, 조회 메서드 정의
  - [x] 도메인 엔티티를 사용하는 인터페이스
- [x] `PointUsageDetailPersistencePort` 인터페이스
  - [x] Application 레이어의 `application.port.output.persistence` 패키지에 위치
  - [x] 저장, 조회 메서드 정의
  - [x] 도메인 엔티티를 사용하는 인터페이스
- [x] `PointConfigPort` 인터페이스
  - [x] Application 레이어의 `application.port.output.config` 패키지에 위치
  - [x] 설정 조회 메서드 정의
- [x] `PointKeyGenerator` 인터페이스 (옵션)
  - [x] 포인트 키 생성 인터페이스
  - [x] `generate()` 메서드 정의

##### 4.3 포인트 키 생성기 구현

- [x] `UUIDPointKeyGenerator` 구현
  - [x] UUID 기반 포인트 키 생성

#### 체크리스트

- [x] 모든 Use Case 인터페이스가 정의되었는가?
- [x] 아웃바운드 포트 인터페이스가 Application 레이어에 올바르게 위치하는가?
- [x] 포트 인터페이스가 도메인 모델을 사용하는가?
- [x] 포인트 키 생성기가 정상적으로 동작하는가? (옵션)

#### 참고 문서

- [`docs/04-API-설계.md`](../docs/04-API-설계.md)
- [`docs/07-프로젝트-구조-설계.md`](../docs/07-프로젝트-구조-설계.md)

---

### 5단계: 애플리케이션 서비스 구현

**목표**: Use Case 인터페이스 구현 및 비즈니스 로직 조합

#### 개발 항목

##### 5.1 Request/Response DTO 정의

- [x] Presentation 레이어의 `presentation.web.dto` 패키지에 위치
- [x] Request DTO (`presentation.web.dto.request` 패키지)
  - [x] `AccumulatePointRequest` DTO
  - [x] `CancelAccumulationRequest` DTO
  - [x] `UsePointRequest` DTO
  - [x] `CancelUsageRequest` DTO
- [x] Response DTO (`presentation.web.dto.response` 패키지)
  - [x] `AccumulatePointResponse` DTO
  - [x] `CancelAccumulationResponse` DTO
  - [x] `UsePointResponse` DTO
  - [x] `CancelUsageResponse` DTO
  - [x] `PointBalanceResponse` DTO
  - [x] `PointUsageHistoryResponse` DTO
  - [x] `PointUsageHistoryItem` DTO
  - [x] `PageRequest` DTO (페이징)
  - [x] `PageResponse` DTO (페이징)
- [x] DTO 매퍼 (`presentation.web.mapper` 패키지)
  - [x] `PointDtoMapper` 구현

##### 5.2 Use Case 구현

- [x] `PointAccumulationService` 구현 (`PointAccumulationUseCase` 구현)
  - [x] Application 레이어의 `application.service` 패키지에 위치
  - [x] 아웃바운드 포트 인터페이스 주입 (`PointAccumulationPersistencePort`, `PointConfigPort`)
  - [x] `accumulate()` 메서드 구현
    - [x] 적립 금액 검증
    - [x] 최대 적립 금액 검증 (설정 조회)
    - [x] 최대 보유 금액 검증 (설정 조회)
    - [x] 만료일 검증
    - [x] 포인트 적립 로직
    - [x] 포인트 키 생성
    - [x] 저장 처리 (Persistence Port 사용)
  - [x] `cancelAccumulation()` 메서드 구현
    - [x] 적립 건 조회 (Persistence Port 사용)
    - [x] 취소 가능 여부 확인
    - [x] 적립 취소 로직
    - [x] 상태 변경 처리
- [x] `PointUsageService` 구현 (`PointUsageUseCase` 구현)
  - [x] Application 레이어의 `application.service` 패키지에 위치
  - [x] 아웃바운드 포트 인터페이스 주입 (`PointAccumulationPersistencePort`, `PointUsagePersistencePort`)
  - [x] `use()` 메서드 구현
    - [x] 사용 가능 잔액 확인 (Persistence Port 사용)
    - [x] 포인트 사용 우선순위 선택 (도메인 서비스 사용)
    - [x] 포인트 사용 처리
    - [x] 1원 단위 사용 상세 내역 생성
    - [x] 사용 건 저장 (Persistence Port 사용)
- [x] `PointCancellationService` 구현 (`PointCancellationUseCase` 구현)
  - [x] Application 레이어의 `application.service` 패키지에 위치
  - [x] 아웃바운드 포트 인터페이스 주입 (`PointUsagePersistencePort`, `PointAccumulationPersistencePort`)
  - [x] `cancelUsage()` 메서드 구현
    - [x] 사용 건 조회 (Persistence Port 사용)
    - [x] 취소 가능 여부 확인
    - [x] 사용 상세 내역 조회 (Persistence Port 사용)
    - [x] 만료 포인트 확인 및 신규 적립 처리
    - [x] 포인트 복원 처리
    - [x] 사용 취소 로직
- [x] `PointQueryService` 구현 (`PointQueryUseCase` 구현)
  - [x] Application 레이어의 `application.service` 패키지에 위치
  - [x] 아웃바운드 포트 인터페이스 주입 (`PointAccumulationPersistencePort`, `PointUsagePersistencePort`)
  - [x] `getBalance()` 메서드 구현
    - [x] 포인트 잔액 조회 (Persistence Port 사용)
    - [x] 적립 내역 조회 (Persistence Port 사용)
  - [x] `getUsageHistory()` 메서드 구현
    - [x] 포인트 사용 내역 조회 (페이징, Persistence Port 사용)
    - [x] 주문번호 필터링 지원

##### 5.3 설정 조회 서비스 구현

- [x] `PointConfigService` 구현
  - [x] 설정 조회 로직
  - [ ] 설정 캐싱 (옵션)

#### 체크리스트

- [x] 모든 Use Case 인터페이스가 올바르게 구현되었는가?
- [x] 비즈니스 규칙 검증이 정확한가?
- [x] 포인트 사용 우선순위가 정확히 적용되는가?
- [x] 만료 포인트 처리 로직이 정확한가?
- [x] 1원 단위 추적이 정확히 동작하는가?
- [x] 트랜잭션 처리가 올바른가?

#### 테스트 항목

- [x] 각 Use Case 단위 테스트 작성 (PointAccumulationServiceTest, PointUsageServiceTest, PointCancellationServiceTest, PointQueryServiceTest, PointConfigServiceTest - 총 37개 테스트 케이스, BehaviorSpec 사용)
- [ ] 통합 테스트 작성 (시나리오 기반)

#### 참고 문서

- [`docs/04-API-설계.md`](../docs/04-API-설계.md)
- [`docs/05-비즈니스-로직-설계.md`](../docs/05-비즈니스-로직-설계.md)
- [`docs/06-설정-관리-설계.md`](../docs/06-설정-관리-설계.md)

---

### 6단계: 웹 어댑터 구현

**목표**: REST API 컨트롤러 및 예외 처리 구현

#### 개발 항목

##### 6.1 REST API 컨트롤러 구현

- [x] `PointAccumulationController` 구현
  - [x] Presentation 레이어의 `presentation.web.controller` 패키지에 위치
  - [x] 인바운드 포트 인터페이스 주입 (`PointAccumulationUseCase`)
  - [x] `POST /api/points/accumulate` - 포인트 적립
  - [x] `POST /api/points/accumulate/{pointKey}/cancel` - 포인트 적립 취소
- [x] `PointUsageController` 구현
  - [x] Presentation 레이어의 `presentation.web.controller` 패키지에 위치
  - [x] 인바운드 포트 인터페이스 주입 (`PointUsageUseCase`)
  - [x] `POST /api/points/use` - 포인트 사용
- [x] `PointCancellationController` 구현
  - [x] Presentation 레이어의 `presentation.web.controller` 패키지에 위치
  - [x] 인바운드 포트 인터페이스 주입 (`PointCancellationUseCase`)
  - [x] `POST /api/points/use/{pointKey}/cancel` - 포인트 사용 취소
- [x] `PointQueryController` 구현
  - [x] Presentation 레이어의 `presentation.web.controller` 패키지에 위치
  - [x] 인바운드 포트 인터페이스 주입 (`PointQueryUseCase`) 
  - [x] `GET /api/points/balance/{memberId}` - 포인트 잔액 조회
  - [x] `GET /api/points/history/{memberId}` - 포인트 사용 내역 조회

##### 6.2 API 공통 응답 구현

- [x] `ApiResponse` 공통 응답 클래스
  - [x] 성공 응답 형식
  - [x] 에러 응답 형식

##### 6.3 예외 처리 구현

- [x] `GlobalExceptionHandler` 구현
  - [x] 도메인 예외 처리
  - [x] 검증 예외 처리 (`@Valid` 오류)
  - [x] 공통 에러 응답 형식

##### 6.4 요청 검증

- [x] DTO에 `@Valid` 어노테이션 적용
- [x] 검증 규칙 정의
  - [x] 금액 검증 (최소값, 최대값)
  - [x] 만료일 검증
  - [x] 필수 필드 검증

##### 6.5 API 문서화

- [x] Swagger 어노테이션 추가
  - [x] `@Operation` - API 설명
  - [x] `@ApiResponse` - 응답 예시
  - [x] `@Parameter` - 파라미터 설명

#### 체크리스트

- [x] 모든 API 엔드포인트가 정상적으로 동작하는가?
- [x] 요청 검증이 올바르게 동작하는가?
- [x] 예외 처리가 적절히 구현되었는가?
- [x] Swagger UI에서 API 문서를 확인할 수 있는가?
- [x] 공통 응답 형식이 일관되게 적용되었는가?

#### 테스트 항목

- [x] REST API 테스트 작성 (MockMvc)
- [x] 예외 처리 테스트 작성

#### 참고 문서

- [`docs/04-API-설계.md`](../docs/04-API-설계.md)

---

### 7단계: 설정 관리 구현

**목표**: 동적 설정 관리 기능 구현

#### 개발 항목

##### 7.1 설정 조회 API 구현

- [x] `PointConfigController` 구현
  - [x] `GET /api/admin/points/config` - 모든 설정 조회
  - [x] `GET /api/admin/points/config/{configKey}` - 특정 설정 조회
- [x] 설정 업데이트 API 구현
  - [x] `PUT /api/admin/points/config/{configKey}` - 설정 업데이트

##### 7.2 설정 캐싱 구현

- [x] 설정 값 캐싱 로직 (`@Cacheable` 적용)
- [x] 캐시 무효화 로직 (`@CacheEvict` 적용)
- [x] `CacheConfig` 클래스 구현

##### 7.3 추가 구현 항목

- [x] `PointConfigValidator` 서비스 구현 (설정 값 검증 및 의존성 검증)
- [x] `PointConfigHistory` 도메인 엔티티 및 JPA 엔티티 구현
- [x] `PointConfigHistory` 리포지토리, 포트, 어댑터 구현
- [x] 설정 관련 도메인 예외 구현 (ConfigNotFoundException, InvalidConfigValueException, InvalidConfigKeyException)
- [x] `PointConfigService.updateConfig()` 메서드 구현
- [x] `GlobalExceptionHandler`에 설정 관련 예외 처리 추가
- [x] Request/Response DTO 구현 (UpdateConfigRequest, PointConfigResponse)
- [x] `PointDtoMapper`에 설정 관련 매핑 메서드 추가

#### 체크리스트

- [x] 설정 조회가 정상적으로 동작하는가?
- [x] 설정 변경 시 동적으로 반영되는가? (캐시 무효화 포함)
- [x] 설정 값 검증이 올바르게 동작하는가?
- [x] 설정 변경 이력이 정상적으로 기록되는가?
- [x] 모든 API 엔드포인트가 정상적으로 동작하는가?

#### 테스트 항목

- [x] `PointConfigValidatorTest` 단위 테스트 작성
- [x] `PointConfigServiceTest` 확장 (updateConfig 메서드 테스트)
- [x] `PointConfigControllerTest` API 테스트 (MockMvc)
- [x] `PointConfigHistoryAdapterTest` 통합 테스트

#### 참고 문서

- [`docs/06-설정-관리-설계.md`](../docs/06-설정-관리-설계.md)

---

### 8단계: 테스트 구현

**목표**: 포괄적인 테스트 코드 작성

#### 개발 항목

##### 8.1 단위 테스트

- [x] 도메인 엔티티 테스트
  - [x] `PointAccumulationTest`
  - [x] `PointUsageTest`
  - [x] `PointUsageDetailTest`
  - [x] `PointConfigTest`
- [x] 값 객체 테스트
  - [x] `MoneyTest`
  - [x] `PointKeyTest`
  - [x] `OrderNumberTest`
- [x] 도메인 서비스 테스트
  - [x] `PointUsagePriorityServiceTest`
- [x] 애플리케이션 서비스 테스트
  - [x] `PointAccumulationServiceTest` (BehaviorSpec, 9개 테스트 케이스)
  - [x] `PointUsageServiceTest` (BehaviorSpec, 6개 테스트 케이스)
  - [x] `PointCancellationServiceTest` (BehaviorSpec, 6개 테스트 케이스)
  - [x] `PointQueryServiceTest` (BehaviorSpec, 6개 테스트 케이스)
  - [x] `PointConfigServiceTest` (BehaviorSpec, 10개 테스트 케이스)
  - [x] `PointConfigValidatorTest`

##### 8.2 통합 테스트

- [x] Persistence Adapter 통합 테스트
  - [x] `PointAccumulationPersistenceAdapterTest`
  - [x] `PointUsagePersistenceAdapterTest`
  - [x] `PointUsageDetailPersistenceAdapterTest`
  - [x] `PointConfigAdapterTest`
  - [x] `PointConfigHistoryAdapterTest`
- [x] JPA 리포지토리 통합 테스트
  - [x] `PointAccumulationJpaRepositoryTest`
  - [x] `PointUsageJpaRepositoryTest`
  - [x] `PointUsageDetailJpaRepositoryTest`
  - [x] `PointConfigJpaRepositoryTest`
- [x] Use Case 통합 테스트
  - [x] 시나리오 기반 테스트
    - [x] 시나리오 1: 기본 적립 및 사용 (`Scenario1BasicAccumulationAndUsageTest`)
    - [x] 시나리오 2: 만료 포인트 처리 (`Scenario2ExpiredPointHandlingTest`)

##### 8.3 API 테스트 (E2E)

- [x] REST API 테스트
  - [x] `PointAccumulationControllerTest`
  - [x] `PointUsageControllerTest`
  - [x] `PointCancellationControllerTest`
  - [x] `PointQueryControllerTest`
  - [x] `PointConfigControllerTest`
- [x] 예외 처리 테스트
  - [x] `GlobalExceptionHandlerTest`
- [x] 검증 오류 테스트 (컨트롤러 테스트에 포함)

##### 8.4 코드 커버리지

- [x] JaCoCo 설정 확인
- [x] 코드 커버리지 목표 달성 (라인 커버리지 80% 이상)
- [x] 커버리지 리포트 확인

#### 체크리스트

- [x] 모든 단위 테스트가 통과하는가?
- [x] 통합 테스트가 통과하는가?
- [x] API 테스트가 통과하는가?
- [x] 코드 커버리지가 목표치 이상인가?
- [x] 모든 예외 시나리오가 테스트되었는가?

#### 참고 문서

- [`docs/09-테스트-전략.md`](../docs/09-테스트-전략.md)

---

### 9단계: 문서화 및 배포 준비

**목표**: 프로젝트 문서화 및 배포 준비

#### 개발 항목

##### 9.1 README 작성

- [x] 프로젝트 개요 작성
- [x] 기술 스택 명시
- [x] 프로젝트 구조 설명
- [x] 빌드 방법 상세 설명
- [x] 실행 방법 및 설정 정보
- [x] API 엔드포인트 목록
- [x] API 사용 예시 (cURL)
- [x] 과제 요구사항 충족 사항 체크리스트
- [x] ERD 및 아키텍처 다이어그램 위치 안내
- [x] 설계 문서 링크

##### 9.2 ERD 다이어그램

- [x] ERD 다이어그램 생성 (PDF 또는 이미지)
- [x] `resource/` 디렉토리에 저장

##### 9.3 AWS 아키텍처 다이어그램 (옵션)

- [x] AWS 아키텍처 다이어그램 생성 (PDF 또는 이미지)
- [x] `resource/` 디렉토리에 저장

##### 9.4 배포 준비

- [x] JAR 파일 빌드 확인
- [x] 실행 스크립트 작성 (옵션)
- [x] Docker 이미지 작성 (옵션)

#### 체크리스트

- [x] README.md가 완성되었는가?
- [x] ERD 다이어그램이 포함되었는가?
- [x] AWS 아키텍처 다이어그램이 포함되었는가? (옵션)
- [x] JAR 파일이 정상적으로 빌드되는가?
- [x] 모든 문서가 최신 상태인가?

#### 참고 문서

- [`docs/10-문서화-계획.md`](../docs/10-문서화-계획.md)
- [`docs/08-AWS-아키텍처-설계.md`](../docs/08-AWS-아키텍처-설계.md)

---

## 단계별 검토 체크리스트

각 단계 완료 후 다음 항목을 검토하여 다음 단계로 진행합니다.

### 공통 검토 항목

- [x] 해당 단계의 모든 개발 항목이 완료되었는가?
- [x] 해당 단계의 모든 체크리스트 항목이 충족되었는가?
- [x] 해당 단계의 테스트 항목이 작성되고 통과했는가? (9단계는 문서화 단계로 테스트 항목 없음)
- [x] 코드 리뷰가 완료되었는가?
- [x] 코드 스타일 가이드를 준수하는가?
- [x] Git 커밋 메시지가 명확한가?

### 단계별 특화 검토 항목

#### 1단계 완료 시

- [x] 프로젝트 구조가 헥사고날 아키텍처 원칙에 맞는가?
- [x] 모든 의존성이 올바르게 설정되었는가?

#### 2단계 완료 시

- [x] 도메인 모델이 비즈니스 요구사항을 정확히 반영하는가?
- [x] 모든 비즈니스 규칙이 도메인 엔티티에 캡슐화되었는가?

#### 3단계 완료 시

- [x] JPA 엔티티가 도메인 엔티티와 올바르게 매핑되는가?
- [x] 리포지토리 쿼리가 효율적인가?

#### 4단계 완료 시

- [x] 포트 인터페이스가 명확하게 정의되었는가?
- [x] 인바운드 포트가 Application 레이어에 올바르게 위치하는가?
- [x] 아웃바운드 포트가 Application 레이어에 올바르게 위치하는가?
- [x] 의존성 방향이 올바른가? (Domain ← Application ← Infrastructure)

#### 5단계 완료 시

- [x] 모든 비즈니스 로직이 올바르게 구현되었는가?
- [x] 트랜잭션 처리가 적절한가?

#### 6단계 완료 시

- [x] API 스펙이 설계 문서와 일치하는가?
- [x] 예외 처리가 적절한가?

#### 7단계 완료 시

- [x] 동적 설정이 정상적으로 동작하는가?
- [x] 설정 캐싱이 올바르게 동작하는가?
- [x] 설정 변경 이력이 정상적으로 기록되는가?
- [x] 설정 값 검증이 올바르게 동작하는가?

#### 8단계 완료 시

- [x] 코드 커버리지가 목표치 이상인가?
- [x] 모든 경계 조건이 테스트되었는가?

#### 9단계 완료 시

- [x] 모든 요구사항이 충족되었는가?
- [x] 프로젝트가 배포 준비가 되었는가?

---

## 개발 진행 상황

각 단계의 진행 상황을 아래에 표시합니다.

| 단계 | 상태 | 완료일 | 비고 |
|------|------|--------|------|
| 1단계: 프로젝트 초기 설정 및 인프라 구축 | ✅ 완료 | 2025-01-22 | 모든 의존성 추가 및 패키지 구조 생성 완료 |
| 2단계: 도메인 모델 구현 | ✅ 완료 | 2025-01-22 | 모든 도메인 모델 및 단위 테스트 작성 완료 (87개 테스트 케이스 통과) |
| 3단계: 인프라스트럭처 레이어 구현 | ✅ 완료 | 2025-01-22 | JPA 엔티티, 리포지토리, Mapper, 포트 인터페이스, DataInitializer, Persistence Adapter 및 통합 테스트 완료 |
| 4단계: 포트 레이어 구현 | ✅ 완료 | 2025-01-22 | 인바운드 포트 4개, PointKeyGenerator 인터페이스 및 UUIDPointKeyGenerator 구현 완료 |
| 5단계: 애플리케이션 서비스 구현 | ✅ 완료 | 2025-01-22 | 모든 Use Case 서비스 구현 및 단위 테스트 완료 (37개 테스트 케이스 통과, BehaviorSpec 사용) |
| 6단계: 웹 어댑터 구현 | ✅ 완료 | 2025-01-23 | REST API 컨트롤러 4개, 공통 응답, 예외 처리, Swagger 문서화, 테스트 완료 |
| 7단계: 설정 관리 구현 | ✅ 완료 | 2025-01-23 | 설정 조회/업데이트 API, 캐싱, 검증, 변경 이력, 테스트 완료 |
| 8단계: 테스트 구현 | ✅ 완료 | 2025-01-23 | 모든 단위 테스트, 통합 테스트, API 테스트, 시나리오 테스트 완료. JaCoCo 설정 및 커버리지 리포트 생성 완료. |
| 9단계: 문서화 및 배포 준비 | ✅ 완료 | 2025-01-23 | README.md 완성, ERD/AWS 아키텍처 다이어그램 생성, JAR 빌드 확인, 실행 스크립트 및 Docker 파일 작성 완료 |

**상태 표기**
- ⬜ 미시작
- 🟡 진행 중
- ✅ 완료

---

## 다음 단계

각 단계 완료 후:

1. **검토**: 해당 단계의 검토 체크리스트 확인
2. **승인**: 모든 검토 항목이 충족되었는지 확인
3. **커밋**: 변경 사항을 Git에 커밋 (필요 시)
4. **다음 단계 진행**: 다음 단계로 진행

---

**작성일**: 2025-01-XX  
**최종 수정일**: 2025-01-XX

