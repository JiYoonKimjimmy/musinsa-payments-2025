# 10. 문서화 계획

## 1. 개요

무료 포인트 시스템의 문서화 계획을 수립합니다. README.md에 빌드 방법, 실행 방법, API 사용 예시, 과제 요구사항 충족 사항을 포함합니다.

## 2. README.md 구조

### 2.1 README.md 목차

```markdown
# 무신사페이먼츠 포인트 시스템

## 목차
1. 프로젝트 개요
2. 기술 스택
3. 프로젝트 구조
4. 빌드 방법
5. 실행 방법
6. API 문서
7. 과제 요구사항 충족 사항
8. ERD 및 아키텍처
9. 개발 가이드
```

## 3. README.md 상세 내용

### 3.1 프로젝트 개요

```markdown
## 프로젝트 개요

무신사페이먼츠 무료 포인트 시스템은 사용자에게 포인트를 적립하고, 사용하며, 취소할 수 있는 기능을 제공하는 백엔드 API 시스템입니다.

### 주요 기능
- 포인트 적립
- 포인트 적립 취소
- 포인트 사용
- 포인트 사용 취소
- 포인트 잔액 조회
- 포인트 사용 내역 조회
```

### 3.2 기술 스택

```markdown
## 기술 스택

### 개발 환경
- Java 21
- Spring Boot 3.x
- Spring Data JPA
- H2 Database (개발 환경)

### 빌드 도구
- Gradle 8.x

### 테스트
- JUnit 5
- AssertJ
- Mockito

### 문서화
- SpringDoc OpenAPI 3 (Swagger)
```

### 3.3 프로젝트 구조

```markdown
## 프로젝트 구조

본 프로젝트는 헥사고날 아키텍처(Hexagonal Architecture)를 적용하여 설계되었습니다.

```
com.musinsa.payments.point
├── domain              # 도메인 레이어
│   ├── entity          # 도메인 엔티티
│   ├── valueobject     # 값 객체
│   ├── repository      # 리포지토리 인터페이스
│   ├── service         # 도메인 서비스
│   └── exception       # 도메인 예외
├── port                # 포트 레이어
│   ├── in              # 인바운드 포트
│   └── out             # 아웃바운드 포트
├── adapter             # 어댑터 레이어
│   ├── in              # 인바운드 어댑터
│   │   └── web         # REST API 컨트롤러
│   └── out             # 아웃바운드 어댑터
│       └── persistence # JPA 구현
└── service             # 애플리케이션 서비스
```

자세한 설계 문서는 `docs/` 디렉토리를 참고하세요.
```

### 3.4 빌드 방법

```markdown
## 빌드 방법

### 사전 요구사항
- JDK 21 이상
- Gradle 8.x 이상 (또는 Gradle Wrapper 사용)

### 빌드 명령어

```bash
# 프로젝트 클론
git clone <repository-url>
cd musinsa-payments-2025

# Gradle Wrapper를 사용한 빌드
./gradlew build

# 테스트 제외하고 빌드
./gradlew build -x test

# JAR 파일 생성
./gradlew bootJar
```

### 빌드 결과물
- `build/libs/musinsa-payments-point-1.0.0.jar`: 실행 가능한 JAR 파일
```

### 3.5 실행 방법

```markdown
## 실행 방법

### 로컬 실행

```bash
# JAR 파일 실행
java -jar build/libs/musinsa-payments-point-1.0.0.jar

# 또는 Gradle로 직접 실행
./gradlew bootRun
```

### 애플리케이션 설정

애플리케이션은 기본적으로 다음 포트에서 실행됩니다:
- **포트**: 8080
- **H2 콘솔**: http://localhost:8080/h2-console
- **Swagger UI**: http://localhost:8080/swagger-ui.html

### 데이터베이스 접속 정보 (H2)

- **JDBC URL**: jdbc:h2:mem:testdb
- **Username**: sa
- **Password**: (비밀번호 없음)

### 초기 데이터

애플리케이션 시작 시 다음 설정이 자동으로 초기화됩니다:
- MAX_ACCUMULATION_AMOUNT_PER_TIME: 100000
- MAX_BALANCE_PER_MEMBER: 10000000
- DEFAULT_EXPIRATION_DAYS: 365
- MIN_EXPIRATION_DAYS: 1
- MAX_EXPIRATION_DAYS: 1824
```

### 3.6 API 문서

```markdown
## API 문서

### Swagger UI

애플리케이션 실행 후 다음 URL에서 API 문서를 확인할 수 있습니다:
- http://localhost:8080/swagger-ui.html

### 주요 API 엔드포인트

#### 1. 포인트 적립
- **POST** `/api/points/accumulate`
- **Request Body**:
```json
{
  "memberId": 1,
  "amount": 1000,
  "expirationDays": 365,
  "isManualGrant": false
}
```

#### 2. 포인트 적립 취소
- **POST** `/api/points/accumulate/{pointKey}/cancel`
- **Path Parameter**: `pointKey` - 취소할 적립 건의 포인트 키

#### 3. 포인트 사용
- **POST** `/api/points/use`
- **Request Body**:
```json
{
  "memberId": 1,
  "orderNumber": "A1234",
  "amount": 1200
}
```

#### 4. 포인트 사용 취소
- **POST** `/api/points/use/{pointKey}/cancel`
- **Request Body**:
```json
{
  "amount": 1100,
  "reason": "주문 취소"
}
```

#### 5. 포인트 잔액 조회
- **GET** `/api/points/balance/{memberId}`

#### 6. 포인트 사용 내역 조회
- **GET** `/api/points/history/{memberId}?page=0&size=20`

### API 사용 예시

#### cURL 예시

```bash
# 포인트 적립
curl -X POST http://localhost:8080/api/points/accumulate \
  -H "Content-Type: application/json" \
  -d '{
    "memberId": 1,
    "amount": 1000,
    "expirationDays": 365,
    "isManualGrant": false
  }'

# 포인트 사용
curl -X POST http://localhost:8080/api/points/use \
  -H "Content-Type: application/json" \
  -d '{
    "memberId": 1,
    "orderNumber": "A1234",
    "amount": 1200
  }'

# 포인트 잔액 조회
curl -X GET http://localhost:8080/api/points/balance/1
```
```

### 3.7 과제 요구사항 충족 사항

```markdown
## 과제 요구사항 충족 사항

### ✅ 필수 요구사항

#### 1. 기능 구현
- [x] 포인트 적립
  - [x] 1회 적립 가능 범위: 1원 이상, 10만원 이하 (동적 설정)
  - [x] 개인별 최대 보유 금액 제한 (동적 설정)
  - [x] 1원 단위 포인트 사용 추적
  - [x] 수기 지급 포인트 구분
  - [x] 만료일 관리 (1일 이상, 5년 미만, 기본 365일)
- [x] 포인트 적립 취소
  - [x] 전체 금액 취소 (사용된 금액이 있으면 취소 불가)
- [x] 포인트 사용
  - [x] 주문번호와 함께 사용 내역 기록
  - [x] 사용 우선순위: 수기 지급 > 만료일 짧은 순
- [x] 포인트 사용 취소
  - [x] 전체/부분 취소 지원
  - [x] 만료된 포인트 사용 취소 시 신규 적립 처리

#### 2. 기술 스택
- [x] Java 21
- [x] Spring Boot 3.x
- [x] H2 Database

#### 3. 문서화
- [x] ERD 다이어그램 (resource 폴더)
- [x] AWS 아키텍처 다이어그램 (resource 폴더, 옵션)
- [x] README.md에 빌드 방법 및 과제 설명

### 📋 구현 상세

#### 동적 설정 관리
- `PointConfig` 엔티티를 통해 다음 설정을 동적으로 관리:
  - `MAX_ACCUMULATION_AMOUNT_PER_TIME`: 1회 최대 적립 금액
  - `MAX_BALANCE_PER_MEMBER`: 개인별 최대 보유 금액
  - `DEFAULT_EXPIRATION_DAYS`: 기본 만료일
  - `MIN_EXPIRATION_DAYS`: 최소 만료일
  - `MAX_EXPIRATION_DAYS`: 최대 만료일

#### 포인트 추적
- `PointUsageDetail` 엔티티를 통해 1원 단위로 포인트 사용 추적
- 어떤 적립 건에서 얼마를 사용했는지 상세히 기록

#### 수기 지급 포인트
- `PointAccumulation.isManualGrant` 필드로 구분
- 포인트 사용 시 수기 지급 포인트가 우선 사용됨

#### 만료일 관리
- 모든 포인트는 만료일을 가짐
- 만료일 범위: 1일 이상, 5년(1824일) 미만
- 기본 만료일: 365일

#### 사용 우선순위
1. 수기 지급 포인트 (`isManualGrant = true`)
2. 만료일이 짧은 순 (FIFO)

#### 만료 포인트 처리
- 사용 취소 시점에 이미 만료된 포인트는 신규 적립으로 처리
- 새로운 pointKey로 적립 처리
```

### 3.8 ERD 및 아키텍처

```markdown
## ERD 및 아키텍처

### ERD 다이어그램
- 위치: `resource/erd.pdf` 또는 `resource/erd.png`
- ERD 설계 문서: `docs/02-ERD-설계.md`

### AWS 아키텍처 다이어그램
- 위치: `resource/aws-architecture.pdf` 또는 `resource/aws-architecture.png`
- 아키텍처 설계 문서: `docs/08-AWS-아키텍처-설계.md`

### 설계 문서
모든 설계 문서는 `docs/` 디렉토리에 있습니다:
- `01-도메인-분석.md`: 도메인 분석 및 요구사항 정리
- `02-ERD-설계.md`: 데이터베이스 설계
- `03-도메인-모델-설계.md`: 도메인 모델 설계
- `04-API-설계.md`: REST API 설계
- `05-비즈니스-로직-설계.md`: 비즈니스 로직 설계
- `06-설정-관리-설계.md`: 설정 관리 설계
- `07-프로젝트-구조-설계.md`: 헥사고날 아키텍처 기반 프로젝트 구조
- `08-AWS-아키텍처-설계.md`: AWS 아키텍처 설계
- `09-테스트-전략.md`: 테스트 전략
- `10-문서화-계획.md`: 문서화 계획
```

### 3.9 개발 가이드

```markdown
## 개발 가이드

### 코드 스타일
- Google Java Style Guide 준수
- Checkstyle 플러그인 사용

### 테스트 실행
```bash
# 모든 테스트 실행
./gradlew test

# 커버리지 리포트 생성
./gradlew test jacocoTestReport
```

### 코드 커버리지
- 목표: 라인 커버리지 80% 이상
- 리포트 위치: `build/reports/jacoco/test/html/index.html`
```

## 4. 추가 문서

### 4.1 API 문서 (OpenAPI/Swagger)

- **자동 생성**: SpringDoc OpenAPI 3 사용
- **위치**: `/swagger-ui.html`
- **스펙**: `/v3/api-docs`

### 4.2 JavaDoc

- **도메인 모델**: 모든 도메인 엔티티와 값 객체에 JavaDoc 작성
- **서비스**: 모든 서비스 메서드에 JavaDoc 작성
- **생성**: `./gradlew javadoc`

## 5. 문서 업데이트 계획

### 5.1 업데이트 시점

- **기능 추가 시**: README.md의 API 문서 업데이트
- **설정 변경 시**: 설정 관리 섹션 업데이트
- **아키텍처 변경 시**: 프로젝트 구조 섹션 업데이트

### 5.2 문서 버전 관리

- 문서 변경 시 Git 커밋 메시지에 문서 업데이트 내용 포함
- 주요 변경사항은 CHANGELOG.md에 기록

### 5.3 버전 관리 전략 (Semantic Versioning)

```
MAJOR.MINOR.PATCH

MAJOR: 호환되지 않는 API 변경
MINOR: 백워드 호환 기능 추가
PATCH: 백워드 호환 버그 수정

예시:
- v1.0.0: 초기 릴리스
- v1.1.0: 새 기능 추가 (백워드 호환)
- v1.0.1: 버그 수정
- v2.0.0: Breaking Change 포함
```

### 5.4 CHANGELOG.md 포맷

```markdown
# Changelog

모든 주요 변경사항이 이 파일에 기록됩니다.
형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.0.0/)를 따릅니다.

## [Unreleased]

### Added
- 추가된 기능

### Changed
- 변경된 기능

### Fixed
- 수정된 버그

---

## [1.1.0] - 2025-02-15

### Added
- 포인트 관리자 API 추가
- 포인트 만료 알림 기능

### Changed
- 포인트 사용 우선순위 알고리즘 개선

### Fixed
- 동시 사용 시 잔액 불일치 버그 수정

---

## [1.0.0] - 2025-01-15

### Added
- 포인트 적립 API
- 포인트 적립 취소 API
- 포인트 사용 API
- 포인트 사용 취소 API
- 포인트 잔액 조회 API
- 포인트 사용 내역 조회 API
```

### 5.5 API 백워드 호환성 정책

- **Deprecation 기간**: 최소 2개 마이너 버전 (또는 3개월)
- **Breaking Change 공지**: 최소 1개월 전 공지
- **버전 관리**: URL 버전 또는 헤더 버전 사용 (`/api/v1/`, `/api/v2/`)

## 6. README.md 작성 체크리스트

- [x] 프로젝트 개요 작성
- [x] 기술 스택 명시
- [x] 프로젝트 구조 설명
- [x] 빌드 방법 상세 설명
- [x] 실행 방법 및 설정 정보
- [x] API 엔드포인트 목록
- [x] API 사용 예시 (cURL, Postman 등)
- [x] 과제 요구사항 충족 사항 체크리스트
- [x] ERD 및 아키텍처 다이어그램 위치 안내
- [x] 설계 문서 링크
- [x] 개발 가이드
- [x] 라이선스 정보 (필요 시)

## 7. 완료

모든 설계 문서가 완성되었습니다. 다음 단계에서는 실제 구현을 진행할 수 있습니다.

---

**목차로 돌아가기**: [01. 도메인 분석](./01-도메인-분석.md)

